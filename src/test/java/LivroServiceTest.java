import org.junit.Test;

import static jdk.internal.org.objectweb.asm.util.CheckClassAdapter.verify;

public class LivroServiceTest {
    @Test
//    public void testCriarLivro() throws SQLException {
//        Connection connectionMock = mock(Connection.class);
//        Statement statementMock = mock(Statement.class);
//
//        LivroService livroService = new LivroService(connectionMock, statementMock);
//
//        when(statementMock.executeUpdate(anyString())).thenReturn(1);
//
//        livroService.criarLivro("Dom Quixote", "1", "1605");
//
//        verify(statementMock).executeUpdate("INSERT INTO livros (titulo, id_autor, ano_publicacao) " +
//                "VALUES ('Dom Quixote', '1', '1605')");
//
//        verify(System.out).println("\nLivro 'Dom Quixote' criado com sucesso!");
//    }
}
